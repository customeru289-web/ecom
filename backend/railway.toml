[build]
builder = "NIXPACKS"

[build.env]
NIXPACKS_NODE_VERSION = "20"

[deploy]
startCommand = "node --import ./crypto-polyfill.js server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
