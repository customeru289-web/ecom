# Local development (npm run dev uses Vite proxy to localhost:5000)
VITE_API_URL=/api

# Production — set this in Vercel Dashboard → Settings → Environment Variables
# VITE_API_URL=https://luxora-api-production.up.railway.app/api
